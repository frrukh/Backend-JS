// Flash Messages:

// setup flash messages:
    // flash messages works upon sessions so it is essential to install and setup express-session first:
        // command : npm i express-session
    // after that we need to install connect-flash package in order to use flash messages.
        // command : npm i connect-flash
    // after this we need to setup connect-flash in app.js file.
    // NOW we are ready to use flash messages.